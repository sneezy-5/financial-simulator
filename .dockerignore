node_modules
server/node_modules
# Le Dockerfile ne construit plus que le backend (voir son commentaire
# d'en-tête) : plus besoin de src/, public/, enterprise-site/, index.html,
# vite.config.mjs ni du .env racine dans le contexte — tout ça reste ignoré.
enterprise-site
src
public
index.html
vite.config.mjs
.env

server/.env
.env.local
.env.*.local
server/.env.*

# Données locales / builds précédents
dist
dist-ssr
dist_electron
server/database.sqlite
server/*.sqlite
server/uploads
uploads

# Fichiers de scratch / debug jamais destinés au déploiement
server/__test_*.js
server/__dump_sheet.js
server/__pdf_to_png.js

# Divers
.git
.vscode
.idea
*.log
tests
node.exe
